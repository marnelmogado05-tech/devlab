# AI

AI architecture, provider abstraction, prompt and safety rules, cost control, and the RAG path.
Operating rules live in the `ai-integration` skill. No AI decision has been recorded yet — the
provider choice and the embedding model both appear under "decisions still to be made" in
[`../adr/README.md`](../adr/README.md), and each must be an ADR before any code assumes it.

_Phase 5. Nothing built yet._
